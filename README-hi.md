यह परियोजना मूल परियोजना से एक फोर्क है - https://github.com/rr-/docstring_parser/



# PyDocSmith

PyDocSmith एक बहुमुखी Python पैकेज है जो विभिन्न शैलियों में डॉकस्ट्रिंग्स को पार्स करने, पता लगाने और रचना करने के लिए डिज़ाइन किया गया है। यह reStructuredText (reST), Google, NumPydoc, और Epydoc सहित कई डॉकस्ट्रिंग सम्मेलनों का समर्थन करता है, जो Python डेवलपर्स के लिए दस्तावेजीकरण प्रथाओं में लचीलापन प्रदान करता है।

## विशेषताएँ

- **डॉकस्ट्रिंग शैली का पता लगाना:** सरल ह्यूरिस्टिक्स का उपयोग करके डॉकस्ट्रिंग्स की शैली (जैसे, reST, Google, NumPydoc, Epydoc) को स्वचालित रूप से पता लगाएं।
- **डॉकस्ट्रिंग पार्स करना:** डॉकस्ट्रिंग्स को संरचित प्रतिनिधित्व में परिवर्तित करें, जिससे दस्तावेजीकरण का विश्लेषण और हेरफेर करना आसान हो जाए।
- **डॉकस्ट्रिंग रचना:** संरचित डॉकस्ट्रिंग्स को वापस टेक्स्ट में रेंडर करें, जिससे स्वचालित डॉकस्ट्रिंग जनरेशन और संशोधन की अनुमति मिले।
- **एट्रिब्यूट डॉकस्ट्रिंग्स:** क्लास और मॉड्यूल स्तर पर परिभाषित एट्रिब्यूट डॉकस्ट्रिंग्स को पार्स करें, क्लास प्रॉपर्टीज और मॉड्यूल-स्तरीय वेरिएबल्स के दस्तावेजीकरण को बढ़ाते हुए।

## स्थापना

```bash
pip install PyDocSmith
```

## उपयोग

### डॉकस्ट्रिंग शैली का पता लगाना

दिए गए टेक्स्ट की डॉकस्ट्रिंग शैली का पता लगाएं:

```python
from PyDocSmith import detect_docstring_style, DocstringStyle

docstring = """
This is an example docstring.
:param param1: Description of param1
:return: Description of return value
"""
style = detect_docstring_style(docstring)
print(style)  # Outputs: DocstringStyle.EPYDOC
```

### डॉकस्ट्रिंग पार्स करना

एक डॉकस्ट्रिंग को उसके घटकों में पार्स करें:

```python
from PyDocSmith import parse, DocstringStyle

parsed_docstring = parse(docstring, style=DocstringStyle.AUTO)
print(parsed_docstring)
```

### डॉकस्ट्रिंग रचना

एक पार्स की गई डॉकस्ट्रिंग को वापस टेक्स्ट में रेंडर करें:

```python
from PyDocSmith import compose

docstring_text = compose(parsed_docstring, style=DocstringStyle.REST)
print(docstring_text)
```

## उन्नत विशेषताएँ

- **ऑब्जेक्ट से पार्स करें:** PyDocSmith Python ऑब्जेक्ट्स से सीधे डॉकस्ट्रिंग्स को पार्स कर सकता है, जिसमें क्लासेस और मॉड्यूल्स शामिल हैं, एट्रिब्यूट डॉकस्ट्रिंग्स को संरचित प्रतिनिधित्व में शामिल करते हुए।
- **कस्टम रेंडरिंग शैलियाँ:** कॉम्पैक्ट या विस्तृत शैलियों के साथ डॉकस्ट्रिंग्स के रेंडरिंग को कस्टमाइज़ करें, और जनरेट किए गए डॉकस्ट्रिंग टेक्स्ट के लिए कस्टम इंडेंटेशन निर्दिष्ट करें।

## docstring_parser के संबंध में संशोधित चीजें

1. डॉकस्ट्रिंग शैली का पता लगाने के लिए बेहतर ह्यूरिस्टिक्स
2. Google डॉकस्ट्रिंग को नोट्स, उदाहरणों को समायोजित करने के लिए संशोधित किया गया है
3. कभी-कभी GoogleDoc स्ट्रिंग में विशेष रूप से जब LLMs जैसे GPT या Mistral से जनरेट किया जाता है तो उचित इंडेंटेशन नहीं होता। PyDocSmith उन खराब डॉकस्ट्रिंग्स को ठीक कर सकता है।
4. GoogleDocstring की विभिन्न शैलियों को समायोजित करने के लिए अतिरिक्त टेस्ट-केस जोड़े गए

मैंने इसे उपयोग के मामले के आधार पर अपडेट किया है - https://www.penify.dev

## योगदान

योगदान स्वागत योग्य हैं! कृपया परियोजना के GitHub पेज पर पुल रिक्वेस्ट सबमिट करें या मुद्दे रिपोर्ट करें।